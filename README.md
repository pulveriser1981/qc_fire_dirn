# QC Fire Dirn Ver 1.0 (Tagged)

This repo includes a pre-created tag `v1.0.0`.  
When you push this repo to GitHub, the GitHub Action will immediately trigger a release build.

## Usage
```bash
git push origin master --tags
```
